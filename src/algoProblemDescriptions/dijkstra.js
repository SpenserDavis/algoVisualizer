import React from "react";

const description = (
  <div className="description">
    <p>
      You are given a two-dimensional array (a matrix), including a "starting"
      cell and a "destination" cell. Your task is to compute the shortest path
      between the two cells.
    </p>
    <p>
      Note that diagonal travel is disallowed, and black cells are
      untraversable.
    </p>
  </div>
);

export default description;
